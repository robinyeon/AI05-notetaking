const add = function (x, y) {
  return x + y;
};

const mul = (a, b) => {
  return a * b;
};

const person = {
  name: "Kim",
  age: 22,
};

const arr = [1, 2, 3];

module.exports = {
  add,
  mul,
  person,
  arr,
};
